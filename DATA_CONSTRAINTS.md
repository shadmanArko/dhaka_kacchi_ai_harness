# Data constraints and portfolio transparency

This is both a real business system for Dhaka Kacchi and a portfolio piece. Keep this file honest
and current: every component that touches data or a model falls into exactly one of three states.
Update it in the same commit that changes the underlying status — it decays fast otherwise.

1. **Real.** Running on genuine Dhaka Kacchi data, genuinely trustworthy.
2. **Stand-in dataset.** Pipeline built and validated, but not yet validated on real Dhaka Kacchi
   data — named public dataset stands in until volume supports the real thing.
3. **Designed, not built.** Documented in `ARCHITECTURE.md` but no code exists yet. Useful for
   design-conversation context; never describe it as working.

| Component | Status | Note |
|---|---|---|
| Order/COGS warehouse (`orders`, `order_line`, `recipe`, `menu_item`, `ingredient`, ...) | Real | Live since the Phase 1 spine (`ARCHITECTURE.md` §9). |
| Review intelligence — sentiment pipeline | Real | Deployed at `reviews.dhakakacchi.com`. |
| Review intelligence — model training data | Stand-in dataset | Yelp dataset stood in for training; live reviews ingest for real going forward. |
| Warehouse `event` / `event_taxonomy` tables | Real | Migrations `0014`-`0016`, applied and verified (`make verify`, `make gate`, `make verify-idempotent`). `0015` added `event.external_id` + `UNIQUE (source, external_id)`, missing from the original `0014` design — needed for idempotent re-ingestion, mirroring `orders`' own `UNIQUE (channel, external_id)`. |
| Attribution backbone (`channel`, `campaign`, `campaign_variant`) | Real | Migrations `0017`-`0019`, applied and verified. `event.channel_id`/`campaign_id`/`campaign_variant_id` FKs wired in `0020`, fulfilling a promise `0014` left as plain nullable columns. Exercised end-to-end locally (2026-09-21): inserted a real channel → campaign → variant → event chain, confirmed every CHECK constraint rejects bad data (invalid `kind`, invalid `status`, `ends_at` before `starts_at`, duplicate `slug`), and confirmed RESTRICT blocks deletion of a channel/campaign/variant that's still referenced. Nothing yet *writes* real channel/campaign/variant rows in production — no ingest job or admin UI creates them; they exist as schema only until organic social ingestion or manual entry starts populating them. |
| `social_post` / `social_metrics_snapshot` schema | Real | Migrations `0021`-`0023`, applied and verified. Exercised end-to-end locally (2026-09-21): a real post with two time-series metric snapshots, confirmed CHECK constraints reject bad `content_type`/negative metrics, and confirmed `CASCADE` correctly removes a post's snapshots (unlike the `RESTRICT` used everywhere else in this section — a snapshot has no independent meaning without its post). |
| Instagram organic ingest job (`warehouse/ingest/instagram.py`) | Real | Meta Developer App created, permissions granted (`instagram_basic`, `instagram_manage_insights`, `pages_show_list`, `pages_read_engagement`), and a full real run executed against the live Dhaka Kacchi Instagram Business Account (2026-09-22 → 23): 132 real posts landed into `social_post`/`social_metrics_snapshot`, `make verify`/`make gate` pass against the resulting dataset. `impressions` confirmed deprecated by Meta as of Graph API v22 (removed from `INSIGHTS_METRICS`); `reach`/`likes`/`comments`/`shares`/`saved` all confirmed working. No formal App Review needed — this only ever accesses the business's own account. |
| Facebook Page organic ingest job (`warehouse/ingest/facebook.py`) | Real | Same Meta App, `read_insights` + `pages_read_user_content` added on top of the Instagram permissions. Full real run against the live Dhaka Kacchi Facebook Page: 229 real posts landed. Comments/shares confirmed to be ordinary post *fields*, not Insights metrics (no `post_comments`/`post_shares` metric exists at all). `post_reactions_by_type_total` confirmed working but returns a nested `{reaction_type: count}` breakdown — only the `"like"` count is mapped to this warehouse's `likes` column; other reaction types aren't tracked. `reach`/`impressions` are NOT yet populated for Facebook (left at 0) — no working metric found for either; `saves` has no Facebook equivalent at all (Instagram-only concept), always 0. |
| Threads organic ingest job (`warehouse/ingest/threads.py`) | Real | Separate Meta app credentials (Threads App ID/Secret, its own token-issuing host `graph.threads.net`), added the Dhaka Kacchi Threads account as a Threads Tester, generated a long-lived token via the app's built-in User Token Generator. Full real run against the live Dhaka Kacchi Threads account (2026-09-23): 57 real posts landed. All six documented insights metrics (`views`, `likes`, `replies`, `reposts`, `quotes`, `shares`) confirmed working live, including the two Meta labels "in development" — no permission gap like Facebook's. `impressions` is sourced from `views` (Meta's closest analog, itself "in development" — lower confidence than the same column for Instagram/Facebook); `reach`/`saves`/`clicks` have no Threads equivalent at all, always 0; `reposts`/`quotes` are real working metrics with no column in `social_metrics_snapshot` to hold them, untracked. Unlike the Facebook Page token, this token is NOT non-expiring — documented 60-day validity, refresh not yet automated. |
| `order_attribution` / `promotion` / `experiment` | Designed, not built | `ARCHITECTURE.md` §4.7. Not yet migrated. |
| Website event capture backend (`dhaka-kacchi-connect`'s `POST /v1/events` + `events` table) | Real | Live in production, tested end-to-end (2026-09-21): valid events insert, invalid input rejects, IP throttle works. |
| Website event capture frontend (`dhaka-kacchi-connect` root `src/`: `analytics.ts`'s `trackWarehouseEvent`, wired to page_view/menu_view/add_to_cart/begin_checkout/purchase/newsletter_signup/social_click/contact) | Real | Deployed to production (2026-09-21), confirmed live (consent banner renders on `dhakakacchi.com`). Smoke-tested end-to-end locally before deploy: page_view, menu_view, add_to_cart, social_click, and the correctly-suppressed failure path all confirmed against a real browser + local DB. Gated on the same consent banner as the existing PostHog analytics — fires nothing for a visitor until they explicitly accept, so real event volume depends on accept rate, not traffic alone. `product_view` and `coupon_used` are NOT wired: no per-item page and no customer-facing coupon field exist to hang them on. |
| Warehouse ingest job reading `dhaka-kacchi-connect`'s `events` table (`warehouse/ingest/events.py`, `raw_events_direct`) | Real | Idempotent (`make verify-ingest-events`), correctly leaves `event.customer_id`/`order_id` NULL while preserving the source's own ids under `properties.source_customer_id`/`source_order_id` (identity resolution is out of scope — see the module's own docstring). Running in production on an hourly cron (`5 * * * *`, see `deploy/CLAUDE.md`) alongside `ingest-direct` and the daily backup — all three were confirmed actually installed and firing on 2026-09-21, after discovering the crontab had gone empty at some earlier, undetermined point. |
| Paid campaign ingestion (Meta/Instagram/TikTok/LinkedIn ad spend) | Designed, not built | No paid campaign exists yet — nothing to ingest until one runs. |
| CLV / churn models | Not built | Gated on order-history volume; will use a public stand-in dataset first per `ARCHITECTURE.md` §4.7's data-sufficiency note. |
| Dish-photo classifier (Vision Transformer) | Real | Existing deployed service, outside this repo. |
